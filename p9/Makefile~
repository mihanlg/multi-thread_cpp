COMPILER         = g++
OPTIONS          = -fopenm-o 

BUILD_LIST += build
BUILD_LIST += run

all: $(BUILD_LIST)

build: main.cpp
	$(COMPILER) $(OPTIONS) sort_openmp main.cpp

exec: exp_mpi
	$(EXECUTOR) ./exp_mpi $(N)

clean:
	rm -rf *.o
